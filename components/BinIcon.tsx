import { Package } from 'lucide-react'

export default function BinIcon({ className = '' }: { className?: string }) {
  return (
    <Package className={className} stroke="#f97316" strokeWidth={1.5} />
  )
}
